import React from "react";

export function FloatingAdd() {
  // Заглушка: если в проекте уже есть реальная реализация, эта заглушка будет просто не использоваться.
  // Если её импортируют в App.tsx — она рендерит пустой контейнер, чтобы не менять текущий UI.
  return null;
}

export default FloatingAdd;
